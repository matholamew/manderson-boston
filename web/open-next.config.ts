import { defineCloudflareConfig } from "@opennextjs/cloudflare";

export default defineCloudflareConfig({
  // This is the default configuration for Next.js 15 on Cloudflare
  // We can add R2 caching here later
});